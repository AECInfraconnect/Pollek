[package]
license = {workspace = true}

name = "dek-policy-router"
version = "0.1.0"
edition = {workspace = true}
repository = {workspace = true}
rust-version = {workspace = true}
authors = {workspace = true}


[dependencies]
dek-policy-runtime = { path = "../dek-policy-runtime" }
dek-resilience = { path = "../dek-resilience" }
dek-cedar = { path = "../dek-cedar" }
dek-openfga = { path = "../dek-openfga" }
dek-errors = { path = "../dek-errors" }
chrono = { workspace = true }
dek-domain-schema = { path = "../dek-domain-schema" }
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
tracing.workspace = true
async-trait = "0.1.89"
metrics = "0.24"
tokio = { workspace = true, features = ["time"] }
dek-capability-registry = { version = "1.0.0-beta.10", path = "../dek-capability-registry" }

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros", "sync", "time", "test-util"] }
criterion = "0.5"

[[bench]]
name = "evaluator_bench"
harness = false

[lints]
workspace = true
